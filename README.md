# -PDF-to-DOCX-Converter
This script converts a PDF file into a Word document (.docx) using the pdf2docx library. Simply specify the input PDF and output DOCX filenames, and the script handles the conversion automatically, preserving the text and layout as much as possible.
